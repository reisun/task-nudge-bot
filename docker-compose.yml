services:
  bot:
    build: .
    env_file: .env
    environment:
      - TOKEN_FILE=/data/.tokens.json
    volumes:
      - bot-data:/data
      - ~/.claude:/root/.claude:ro
    restart: unless-stopped

volumes:
  bot-data:
